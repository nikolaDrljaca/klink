<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    <changeSet id="ddl-klink-entry-entity-notify-change" author="Nikola Drljaca">
        <sql dbms="postgresql" splitStatements="false">
CREATE OR REPLACE FUNCTION notify_klink_entry_change()
    RETURNS TRIGGER AS
$$
BEGIN
    PERFORM pg_notify(
            'klink_entry_change',
            json_build_object(
                    'operation',
                    CASE
                        WHEN TG_OP = 'INSERT' THEN 'inserted'
                        WHEN TG_OP = 'UPDATE' THEN 'updated'
                        WHEN TG_OP = 'DELETE' THEN 'deleted'
                        END,
                    'row', row_to_json(COALESCE(NEW, OLD))
            )::text);
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
        </sql>
    </changeSet>

    <changeSet id="4" author="Nikola Drljaca">
        <sql>
            -- create trigger to run notify function after change
            CREATE TRIGGER klink_entry_change_notify
                AFTER INSERT OR
            DELETE
            ON klink_entry
                FOR EACH ROW EXECUTE FUNCTION notify_klink_entry_change();
        </sql>
    </changeSet>
</databaseChangeLog>
