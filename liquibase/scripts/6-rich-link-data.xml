<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    <changeSet id="ddl-create-rich-link-data" author="Nikola Drljaca">
        <sql dbms="postgresql">
create table if not exists rich_link_data (
    id uuid primary key,
    klink_entry_id uuid
        references klink_entry(id)
        on delete cascade,
    title text,
    description text,
    created_at timestamp default now()
        not null
);
        </sql>
    </changeSet>

</databaseChangeLog>
